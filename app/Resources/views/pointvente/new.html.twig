  {% extends "AppBundle::layout.html.twig" %}

{% block main_content %}
    <div class="main-content">
    <h1>Field soldiers creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('pointvente_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
{% endblock %}
